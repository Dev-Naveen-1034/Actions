name: Hello World Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello World
        run: echo "Hello, world!"

  Bye:
    runs-on: ubuntu-latest

    steps:
      - name: Print Good bye
        run: echo "Bye da Vennai"
     
  
 
